import React from 'react';
import Layout from '../../components/layout';

const AddNewDistribution = () => {
    return (
        <Layout>
            <div></div>
        </Layout>
    );
};

export default AddNewDistribution;